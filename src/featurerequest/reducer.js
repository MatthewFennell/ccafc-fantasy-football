import fp from 'lodash/fp';
import * as actions from './actions';

const initialState = {
};

const featureRequestReducer = (state = initialState, action) => {
    switch (action.type) {
    default:
        return state;
    }
};

export default featureRequestReducer;
