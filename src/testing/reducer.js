const initState = {
  fetchedLeague: false,
  myLeagues: []
};

const authReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;
